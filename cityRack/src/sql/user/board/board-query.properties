listCount=SELECT COUNT(*) FROM BOARD WHERE BO_PNO='1'
selectList=SELECT RNUM, BO_NO , BO_TITLE, M_NAME, BO_DATE FROM(SELECT ROWNUM RNUM, BO_NO , BO_TITLE, M_NAME, BO_DATE FROM(SELECT B.BO_NO ,B.BO_TITLE,M.M_NAME,B.BO_DATE FROM BOARD B JOIN MEMBER M ON(B.M_NO = M.M_NO))) WHERE RNUM BETWEEN ? AND ?